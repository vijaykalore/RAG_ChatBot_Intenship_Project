services:
  - type: web
    name: rag-chatbot
    env: docker
    branch: main
    plan: free
    dockerfilePath: Dockerfile
    region: oregon
    startCommand: gunicorn -w 1 -b 0.0.0.0:$PORT app:app
    envVars:
      - key: PORT
        value: "5000"
